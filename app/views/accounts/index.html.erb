<div class=container>
  <h2>Welcome
    <%= current_user.first_name %>
  </h2>
  <hr />
  <%= link_to 'Add Account', new_account_path %>
    <h3>Your Accounts</h3>

 <%= link_to 'See Comments', account_path(@accounts) %>
</div>
<div class="right-align">
<%= button_to 'LOG OUT', destroy_user_session_path, method: :delete%>
</div>